# 内部开发人员说明

> 对象：接手/维护本项目的内部开发。
> 定位：**索引级**说明，只给"地图 + 最易踩的坑 + 第一动作"。
> 规范唯一源是根目录 `AGENTS.md`（构建红线/命名/同步清单/审计，冲突以它为准，
> 本篇不复述细则，防止双源分叉）。协议唯一源是 `docs/通讯接入.md`。
> 现场用法见 `docs/现场工程师培训手册.md`（改了功能先想手册要不要同步）。

## 一、新人上手路径（按序，半天）

1. 读 `AGENTS.md`（强制前置，角色/红线/构建全在里面）。
2. 读本篇二~四节，建地图；跑一遍回归（第九节），确认环境 OK。
3. 通读 `README.md` 4~6 节（业务流/配置/菜单），对照跑起来的主窗体点一遍。
4. 改第一行代码前，先读第五节（同步清单）——漏一处就是一个现场 bug。

## 二、技术栈与构建

- .NET Framework 4.7.2 / WinForms / C#（**不是** .NET Core，勿引入新语法/API）。
- 关键库：SunnyUI（界面）/ NModbus（Modbus）/ Newtonsoft.Json / System.Management（WMI）。
- 构建（AGENTS 原文，MSBuild 找不到先定位）：
  `MSBuild.exe AgingTestSystem/AgingTestSystem.csproj /p:Configuration=Debug /p:Platform=AnyCPU /t:Build`
- 老式 csproj：**新增 .cs 必须手工登记 `<Compile Include>`**（漏登记报 CS0246），
  纯代码窗体加 `<SubType>Form</SubType>` 即可。
- 文件编码 UTF-8；禁 PowerShell 默认编码写中文文件；不提交运行时数据与机密；
  不主动 commit/push；改完必构建；注释写给小白看（做什么+为什么+怎么改）。

## 三、目录速览（最常改的 12 个，完整表见 README 3 节）

| 文件 | 一句话 |
| :--- | :--- |
| `Views/MainForm.cs` | 主窗体装配/配置加载/项目切换热更 |
| `Services/DeviceManager.cs` | 业务编排核心（只做调用决策+IO+日志，判定逻辑在下行） |
| `Services/AgingSequencer.cs` | **判定类逻辑唯一归宿**（纯函数，先改这里+同步用例） |
| `Models/DeviceConfig.cs` | 设备配置模型（加 App.config 项先加属性） |
| `Models/PolicyEnums.cs` + `Services/ProjectPolicyStore.cs` | 策略枚举（0 值=现状，只能追加）+ Policy.json 分流（PolicyKeys 唯一名单） |
| `Services/ProjectProfile.cs` | 项目档案（跟项目 vs 跟机器路径裁决，见第四节） |
| `Dialogs/SettingsForm.cs` | 设置表（编辑控件按 key 分发，加配置项看第五节） |
| `Views/ProcessPolicyForm.cs` + `Views/PolicyGraph.cs` | 流程驾驶舱（拓扑画死，节点只挂真实 key） |
| `Services/RuleExpr.cs` + `Services/RuleEngine.cs` | 规则引擎（变量冻结 12 个，规则只加严） |
| `Services/MesMapping.cs` + `Services/MesReporter.cs` | MES 映射纯函数 + 上报器（Transport 测试缝 finally 复位） |
| `Services/License/*` + `Dialogs/LicenseForm.cs` | 软件授权（见下文专节，不动私钥） |
| `.opencode/skills/agingtest-regression/` | 回归技能（用例 `tests/TestRunner.cs`，脚本 `scripts/`） |

## 四、运行时文件：跟项目 vs 跟机器（改存储路径先背此表）

| 跟项目（`Projects/<项目>/`，切项目即换） | 跟机器（程序目录，切项目不动） |
| :--- | :--- |
| 配方 `Recipes.json` | 用户 `Users.json`（gitignore，哈希存储） |
| 工位设置 `StationSettings.json` | 任务快照 `TestSession.json`（gitignore） |
| 主页布局 `HomeLayout.json` | 面板布局 `PanelLayout.json` |
| 策略 `Policy.json` / 布局 `PolicyLayout.json` | 日志 `Logs/`、连接缓存（`BarometerPort.cache`/`FanLastIp.cache`） |
| `ReportColumns`/`DisplayModes` 等策略类配置值 | `License.lic` / `License.trial`（gitignore） |
| | `MesQueue.json`（离线缓存） |

改存储路径必须全仓扫字面文件名（老用例字面路径会批量红，V1.67 实锤）。

## 五、三套同步清单（本项目最高频返工点，逐项打勾）

**加 App.config 项（五处）**：`DeviceConfig` 属性 + `App.config` key（含中文注释）
+ `MainForm` 读取 + `SettingsForm`（`_boolKeys` **和** `ValidateValue` 布尔分支两名单
+ 都要进 + `_descriptions` + `_categories`）。另给 `_descriptions` 写清"现状是什么/
改了会怎样"（设置表 tooltip 全覆盖，超 40 字走 `WrapTooltip`）。

**加工艺策略 key（枚举型，另一套）**：`PolicyEnums` 枚举 + `DeviceConfig` 属性
+ `ProjectPolicyStore.PolicyKeys`（唯一名单）+ `EnumOptions` 下拉选项
+ `App.config` key + `MainForm` 读取（`ParsePolicyEnum` 非法兜底现状）
+ `SettingsForm`（`_descriptions`+`_categories`+`ValidateValue`+`CreateValueCell`
下拉分支，不进 `_boolKeys`）。

**配方加字段（三窗链）**：`RecipeConfig` → 录入三窗（`RecipeManagerForm` /
`BatchRecipeForm` / `StationSettingsForm`：输入框+保存+回填+检索回调+头部 ASCII 图）
→ `SetStationRecipe` → `StationInfo`（+`Clone`）→ `ApplyStationInfo`
→ `BarometerData`（+`Clone`）。另注意 `SetStationRecipe` 只认 null=保持，
下发 0 就是定格 0（V1.72.2 血泪）。

## 六、判定逻辑写哪（V1.67 收紧，无例外）

策略执行侧一律先加 `AgingSequencer` **纯函数**并同步用例
（`BuildStartBlockText` / `MapAlarmResult` / `ComputeResumeDurationSeconds` /
`ValidatePolicyCombination` 类），`DeviceManager` 只做"调用决策 + IO + 日志"。
直接写 UI 层的判定=单测够不着，复查必搬（V1.84.1 实锤：破空阀碰撞）。
快照加字段同步列"哪些状态变迁写快照"；持续计时状态变迁必调 `ResetStation`。

## 七、UI 红线摘要（细则见 AGENTS，只记"什么不能干"）

1. SunnyUI 换肤：新窗打开前调一次 `ThemeManager.ApplyTo`；按钮语义色不动；
   `is Button/TextBox/ComboBox` 认不出 Sunny 控件，按类型名走分支。
2. UIForm 标题占 35px：绝对布局下移 35 + 加高 + MinimumSize；居中只调 X 不动 Y。
3. Designer 三军规：`InitializeComponent` 内禁注释；与 VS 口径不一致以重写版为基线；
   `.resx` 别手删；禁 `AddRange(裸标识符)` / `= xxx.Range.Min/Max` /
   `ZoomScaleRect`+`Font` 混搭；纯代码窗补无参构造给预览。
4. 自绘三不：不用 `DoubleBuffered`（TextRenderer 离屏 2.2ms/处）、变换矩阵下不挂
   `TextRenderer`、度量全放布局态；裁剪按图元包围盒判交；坐标一律锚定（`PanelLayoutConfig` 类头）。
5. Dock 的 Fill 必须最后布局（统一 `BringToFront()` 一次）；列表画布缩放抄
   `IoRemapGraphControl`（DPI×zoom 进布局基准）。
6. 动态重建走 `ControlDisposeHelper.DisposeAllAndClear`（禁手写 foreach）；
   非模态 popup 的 FormClosed 里 `finally Dispose`；后台/定时/订阅四模式自查
   R5/R6/R7；新非模态弹窗在 `$SafeShowKeys` 登记。
7. 界面文件头补 ASCII 布局图（每次改界面同步）；tooltip 全覆盖（标签+输入框两边挂）。
8. 叠放取"子控件"方案（挂进宿主 Controls，天然浮上层）；自绘图标不用 emoji；
   未 Show 窗体 `PerformClick` 是空操作，回归直接调 handler。

## 八、落盘与健壮性（V1.84 大扫荡，新增持久化先想这三条）

- 运行时 json 一律 `AtomicFile.WriteAllText`（`File.Replace` + 清残留 tmp），禁裸写。
- 路径一律 `BaseDirectory` 绝对路径；测试隔离走 `BaseDirOverride` 测试缝（finally 复位）。
- 规则解析三防护（`||` 切分枚举合法组合 / 严格变量模式 / 长度嵌套上限）；
  项目名校验只认 `IsValidProfileName`；IO 写失败先写后清；时钟作差负跨度钳零；
  对外只吐副本（User/账号 Clone）；即时写失败留痕（CSV/LOG）。
- 用例假绿三查：探针先验有效、存在性断言看前后差值、新逻辑无用例=没修。

## 九、回归测试（改完代码必须验证）

```powershell
# 日常小改（按 git 改动算模块子集；新文件先在 get_affected_modules.ps1 的 $Map 登记，否则付全量代价）
powershell -ExecutionPolicy Bypass -File .opencode\skills\agingtest-regression\scripts\build_and_test.ps1 -Affected
# 大重构/发布前/改骨架（csproj/Interfaces/用例自身/scripts）跑全量（默认，不带 -Affected）
```

- 修 bug 必先补复现用例（红→修→绿），沉淀进 `tests/TestRunner.cs` 对应模块；
  新用例/冒烟步骤回流 skill（tests/scripts/SKILL.md），禁散落临时目录。
- 覆盖点与加用例步骤见 skill 的 SKILL.md；改 UI 跑终结器审计
 （`.opencode/skills/agingtest-regression/scripts/audit_finalizer_risk.ps1`，HIGH 拦提交）。
- 界面像素级 bug 用全局技能 `winforms-ui-debug`（harness 直启+反射+截图），
  修完把新套路回写 SKILL.md。

## 十、软件授权专节（动授权先读 AGENTS"软件授权铁律"全文）

- 私钥 `tools/LicenseKeyGen/license_private.xml` 永不出公司（gitignore），产品只嵌公钥；
  换公钥=老证全废。`CanonicalString` 改字段必须同步改 KeyGen 手拼串 + 载荷 JSON + 升级 `v`。
- 四道关：机器（四取三，只差一块放行+警告）→ 项目（空=通用）→ 点数（72/36/16）
  → 有效期（过期 7 天宽限）。拷贝文件夹到新机=机器关直接拦（生意防线，别"修"掉）。
- 试用双记取最早（文件+注册表，DPAPI 密文/明文兜底都要能读）；`UpdateRunStamps`
  禁写 MinValue 垃圾日期；测试缝反射赋值+try/finally 复位+GUID 注册表隔离。
- 签发工具中文参数用 cmd 跑（PS5.1 会把中文转 U+FFFD），签完跑 verify。
- 标题栏后缀走 `WithLicenseSuffix` **替换语义**，禁 Contains 追加。

## 十一、提交与文档规范

- 提交信息中文，风格看 `git log`；用户没要求不 commit/push；
  提交前 `git status` + `git diff` 确认范围；推送走 `git自动提交推送` skill
 （precheck + CHANGELOG 取标题 + dry-run 回检，现场目录用 `--explicit`）。
- 每次任务交付前逐份过一遍：`CHANGELOG.md`（顶部新小节：改动范围/为什么/优化点，
  再小也记）/ `README.md`（目录/业务流/构建变了就同步）/ `docs/通讯接入.md`
  （寄存器/协议/IO 映射变了就同步，写明版本）/ 头部 ASCII 图与注释；
  全仓 rg 被改功能名确认无残留引用。
- 本次新增的两篇手册同样：**改了功能先想两篇手册要不要同步**
  （现场手册=操作入口/流程/按钮文字；本篇=地图/清单）；
  沉淀新约定进 `AGENTS.md`（下次任务自动遵守）。

## 十二、授权码签发教程（公司内部，step by step）

> 对象：给客户签发 `License.lic` 的商务/技术。原理与红线只记结论做，
> 判定细节看 AGENTS"软件授权铁律"全文，命令细节看
> `tools/LicenseKeyGen/Program.cs` 头部注释与 `PrintHelp`（两者分叉以代码为准）。

### 0. 先搞清分工（一句话）

- 签发要**私钥**，验签只要公钥。私钥 `tools/LicenseKeyGen/license_private.xml`
  只在公司电脑上，**绝不发给客户**（已 gitignore；丢了=签不了，只能换钥+发版，
  且老证全废——离线 U 盘备份）。
- 客户侧只需要做一件事：在【关于→软件授权】点"导出机器码"，把 txt 发回来。

### 1. 准备工具（第一次，或换电脑）

```cmd
cd tools\LicenseKeyGen
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe Program.cs
```

- 目录里已有 `license_private.xml` 就直接用，**不要**跑 `genkeys`。
- 没有（新电脑/重装）才跑 `KeyGen genkeys`：打印出的公钥贴进产品端
  `LicenseManager.cs` 的 `PublicKeyXml` 后**重新发版**（换钥=老证全废）。

### 2. 每次签发 4 步

| 步 | 动作 | 说明 |
| :--- | :--- | :--- |
| 1 | 拿到机器码 | 客户授权窗"导出机器码"的 txt（分组串 `XXXX-XXXX-…`，4 位一组）。分组串直接粘就行，工具自动去横线空格 |
| 2 | `issue` 签发 | **在 cmd 窗口或 .bat 里跑**（禁 PowerShell 5.1 的 `&` 调用，中文项目名会被转成乱码，工具会硬停），参数见下表 |
| 3 | `verify` 自检 | `KeyGen verify --in <刚签的lic>`，显示"签名有效"才往下走 |
| 4 | 交付导入 | 文件改名回 `License.lic` 发现场；客户侧**管理员**在授权窗导入（操作员只读，按钮点不了先切管理员）；主窗标题栏挂 `[已授权至…]` 即成 |

### 3. `issue` 参数表

| 参数 | 必填 | 说明 |
| :--- | :--- | :--- |
| `--machine` | 是 | 机器码分组串（64 位 hex，工具自动去横线/空格/转小写） |
| `--expiry` | 是 | 到期日，格式必须 `yyyy-MM-dd`（如 `2027-09-01`；过期 7 天内宽限不断线，超 7 天阻断） |
| `--stations` | 是 | 点数，正整数，按 `72/36/16` 档卖（超配阻断） |
| `--project` | 否 | 留空=通用版（不限项目）；给了=限定版（只能跑这个项目，新开项目即拦） |
| `--mes` / `--rules` | 否 | 功能开关，不加=关闭（超范围只警告不阻断，标题栏会提示） |
| `--serial` | 否 | 内部编号（如 `LIC2026001`），追溯用，建议每单编一个 |
| `--out` | 否 | 输出文件名，缺省 `License.lic`；建议先存 `客户简称_到期日.lic` 归档，交付时改回 `License.lic` |

完整例子（cmd 窗口里跑）：

```cmd
KeyGen issue --machine XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX --project 烧屏测试 --expiry 2027-09-01 --stations 72 --mes --serial LIC2026001 --out 某某公司_2027-09-01.lic
KeyGen verify --in 某某公司_2027-09-01.lic
```

### 4. 常见失败对照表（先查这里再问人）

| 现象 | 原因 | 怎么办 |
| :--- | :--- | :--- |
| 参数含乱码 `U+FFFD`，直接硬停 | PowerShell 5.1 传中文参数 | 改 cmd 窗口（`chcp 936`）或写进 `.bat` 双击跑 |
| `--machine` 必须是 64 位十六进制 | 拷多了 txt 里的表头文字 / 没拷全 | 只取分组串那一行 |
| `--expiry` 格式错误 | 不是 `yyyy-MM-dd` | 照格式重写 |
| 找不到 `license_private.xml` | 目录跑错 / 新电脑没拷备份 | 回 `tools/LicenseKeyGen` 目录，或从离线 U 盘拷回 |
| `verify` 报签名无效 | 签发端与产品端规范串分叉 | **停发**，查代码（`CanonicalString` 改字段必须同步改 KeyGen 手拼串+载荷 JSON+升级 `v`） |
| 客户导入后仍被拦 | 对四道关：换整机 / 限定版跑新项目 / 点数超配 / 过期超 7 天 | 按对上的是哪道关重签（只差一块硬件是放行+警告，不用重签） |
| 客户说导入按钮点不了 | 导入仅管理员可用 | 让客户先在【用户权限】切管理员 |
