# 问题确认清单

## 附图：软件里现在跑的就是这个逻辑（单台工位视角，与代码逐条对过）

```
空闲 ──[点启动]──► 只开真空阀，不带电（抽真空Vacuuming阶段）
                          │
         ┌────────────────┼──────────────┐
         │                │              │
    真空到位 且        超时还没到位      连续读失败N次
    延时开启到       （默认15秒可配）   （默认3次可配）
    （等于阈值              │              │
     算到位）               ▼              ▼
         │           报压力越限FAIL   报设备异常
         ▼           关阀＋断电       关阀＋断电
    载台上电，         面板标故障红    面板标故障红
    开始计时           永不带电        不记产品FAIL
    （计时起点=上电时刻，
    参数启动瞬间定格）
         │
         ├──── 老化中失压/掉线同样报警（与抽真空阶段同动作）──►FAIL/设备异常
         │
    时间一到（配方时长优先、全局兜底；都为0=不限时长永不自动完成）
         │
         ▼
    自动下电＋关阀（关阀≠泄压，残压还在，无自动破空/无响铃）
    面板淡钢蓝"已完成"（日志记PASS·待取料）
```

- **停止 vs 复位（易混）**：手动停止只中止Testing/Vacuuming/Aging回空闲，清不掉红/蓝屏；
  故障红/完成蓝必须按“报警复位”或重新扫码绑SN才回空闲。急停=全部输出OFF+停风机，但红/蓝标记同样保留到复位。
- **断电来电**：重启询问“整台重测（满时长重跑，已跑作废） / 放弃（安全关阀断电）”。
- **合格口径**：全程无报警＝PASS；压力越限/超时＝FAIL（产品责任）；通讯失联＝设备异常（不记产品 FAIL）。
- 图里“延时开启/老化时长/负压阈值”配方优先、全局兜底，启动瞬间定格，中途改配方不影响在跑的台。

## 一、问题确认

### 对工艺

1. **真空到底干什么用的？**
   当面问："漏气这台货，你们算治具问题还是产品不良？"
   - 治具问题 → 现状不动，只验阈值参数。
   - 产品不良 → 报警要关联 SN 记不合格，回来改判定逻辑（约 0.5~1 天）。
   追问一句："摆料时产品靠什么固定？一松手掉吗？"掉 → 加回"装夹预吸附"入口（带压力监控）。

2. **载台给产品供什么电？要不要监测电流？**
   当面问："多少V？直流交流？谁供电？老化中看不看电流/功率？"
   - 要回采 → 加电流表通讯，最大工作量项，单独排期，**不当场承诺时间**。

3. **送风是加热还是冷却？温度谁说了算？超温停单台还是全线？**
   - 软件设温 → 现协议无写温度寄存器，需找风机厂商，先记二期。

4. **老化合格谁说了算？在哪看结果？**
   现状口径：全程无报警=PASS、压力报警=FAIL、失联=设备异常（不记产品 FAIL）。
   只确认：接受否 + 判定人 + 结果在哪看（屏幕/报表/MES）。

### 对操作员

5. **一批放多少？做完一个换一个还是一批同进同出？批号怎么定？混批吗？**
6. **到时完成后你们希望怎样？自动下电够吗，要不要自动泄压/响一声？**
7. **报警的货现在怎么处理？复位谁来按？**

### 对质量

8. **报表要什么样？最好直接拿一张现有纸质/Excel 照抄。**
   现状只有 CSV 明细 + 历史查询；直通率/曲线/SN 追溯单都要现做。

### 对电气/IT

9. **载台供电方式 + 真空管路走向 + 送风管道，拍照。**
10. **MES/ERP 有无硬性对接要求？接口形式？**

### 补充确认点

11. **老化中掉真空停不停？**
    现状：抽真空阶段和上电老化中失压/掉线同样关阀断电（压力类=FAIL，失联=设备异常）。只确认接受否。
12. **15秒超时 + 连续3次失联够吗？**
    现状默认值，可在系统设置改。当面测一次从常压抽到阈值要几秒，回填§三参数表。
13. **所有配方启动时间都大于0吗？空SN/空配方让不让启动？**
    现状：配方与全局都为0=不限时长、永不自动完成；空SN/空配方也能点启动。要不要软件拦截？
14. **完成/故障回空闲谁来按？**
    现状：手动停止清不掉红/蓝屏，必须按"报警复位"或重新扫码才回空闲。定死按的人，列入培训。
15. **完成后产品徒手能拿下吗？（关联Q6）**
    现状：关阀≠泄压，无自动破空、无响铃。现场试一次拿不拿得下，决定加破空阀/声光否。
16. **风机断连/超温允不允许继续跑？（关联Q3）**
    现状：风机掉了测试照跑只记一行日志；极限温度默认0=禁用。回填极限温度，定断连阻断否、超温联停否。
17. **DI硬件触点并入报警吗？**
    现状：`UseDiAlarmContact=false`仅显示不联锁。开了要先确认常开/常闭+NPN电平，否则误报/漏报。
18. **定格语义接受吗？**
    现状：启动瞬间抄配方，中途改配方旧台按旧参数跑完。接受 / 不接受（不接受=大改，排期重议）。

## 二、真机验证

- [ ] 通讯测试窗逐点动 DO（电磁阀吸合声/继电器灯）+ 逐点短接验 DI，点表与实物对上
- [ ] 气压表扫描 72 台在线率；离线表列清单交客户换修，不等
- [ ] 送风机测试窗：定值启停 + 温湿度读数正常
- [ ] 小批量真跑 4~8 台：装夹→扫码→配方→启动→真空建立→人为松管验报警联动→到时完成
- [ ] 数据链：CSV 事件 + 历史查询 + 操作日志，问质量够不够
- [ ] 断电策略接受度：整台重测（已实现），不行就重议

## 三、《业务流程确认稿》

**结论（每条一句话 + 签字人）：**

1. 真空用途：__________；漏气责任：治具 / 产品（圈一个）
2. 装夹是否依赖真空吸附：是 / 否；是→加"装夹预吸附"入口
3. 载台供电：______V，直流 / 交流；回采电流：要 / 不要
4. 送风目的：加热 / 冷却；温度设定权：风机面板 / 软件；超温动作：单台 / 全线
5. 合格口径：接受现状 / 改为______；判定人：______
6. 批次：流水 / 整炉；批号规则：______；混批：允许 / 不允许
7. 完成动作：自动下电够 / 加自动泄压 / 加声音提醒
8. 报警货处置：重测 / 报废 / 降级；复位人：______
9. MES 对接：无 / 有（形式______，二期）
10. 报表：CSV 够 / 要 Excel 样张（附照片）
11. 断电重测：接受 / 不接受（不接受→重议）
12. 老化中失压：停机接受 / 不接受
13. 超时__ms / 失联__次：回填参数表
14. 时长>0确认：是 / 否；空SN/空配方拦截：要 / 不要
15. 复位人：______（培训已讲：停止清不掉红/蓝）
16. 取料：徒手能拿 / 要破空阀 / 加声光
17. 风机断连阻断：是 / 否；超温联停：单台 / 全线 / 不停；极限温度：______℃
18. DI触点并入：是 / 否（是→电平已确认______）
19. 定格语义：接受 / 不接受（不接受→重议）

**工艺参数（回填配方用）：**

| 参数 | 现场值 | 说明 |
| --- | --- | --- |
| 负压阈值 kPa | | 配方优先、全局兜底；等于阈值算到位不报警 |
| 延时开启 | | 到位后至少再等这么久才上电；0=到位即上电 |
| 启动计时 | | 配方>0用配方，否则全局；都为0=不限时长永不自动完成 |
| 真空确认超时 ms（默认15000） | | 开阀后多久还没到位判FAIL，现场实测回填 |
| 通讯连续失败次数（默认3） | | 几次读不到判设备异常 |
| 极限温度 | | 软件默认0=禁用，不填永不报超温 |
| 单批时长/每天批数/班次 | | |

**改造清单（排期施工依据）：**

| # | 改什么 | 验收标准 | 工作量 |
| --- | --- | --- | --- |
| 1 | | | |
| 2 | | | |

签认：工艺 ______  操作 ______  质量 ______  日期 ______

---

*背景备查：72 工位真空吸附+载台上电老化线，1 台送风机（TCP:50000），1 把扫码枪；
`①装夹→②扫码→③选配方→④启动（真空到位+延时才上电）→⑤监控→⑥到时自动下电→⑦追溯→⑧异常（快照整台重测）`。
协议细节见 docs/通讯接入.md。*
