# 通讯接入说明（给新手）

本文档说明：如何把“气压表报警 → 上位机联动 IO 输出控制”跑起来，以及通线后应该按什么顺序验证与收口 TODO。

## 1. 总体链路

1. 上位机定时轮询读取气压表压力（Modbus RTU / RS485→USB）
2. 上位机轮询读取 IO 模块 DI/DO（Modbus TCP）
3. 上位机用阈值判断报警（默认阈值：-95000Pa）
4. 报警进入的瞬间（边沿）执行联动输出：
   - 关闭真空电磁阀输出
   - 关闭载台上电输出

核心编排在 [DeviceManager.cs](file:///e:/Project/BarometerWinform/BarometerWinform/Services/DeviceManager.cs)。

## 2. 对应代码与职责

### 2.1 气压表通讯（Modbus RTU）

- 实现类：[ModbusRtuBarometerReader.cs](file:///e:/Project/BarometerWinform/BarometerWinform/Services/ModbusRtuBarometerReader.cs)
- 读取方式：Holding Register（功能码 0x03）
- 从站地址：默认使用 deviceId（1~72）作为从站地址
- 寄存器地址：`BarometerPressureRegisterAddress`（默认 0x0010）

给新手的解释：
- “Modbus 主站/从站”可以理解为：
  - 主站：上位机（主动发起请求）
  - 从站：气压表（被动响应请求）
- “RTU”是串口协议版本：数据帧里有 CRC 校验
- “Holding Register(保持寄存器)”是一种寄存器类型，通常用来放设备的测量值/参数
- “寄存器地址 0x0010”是说明书里的地址，不是 PLC 的 X/Y 地址，两者不要混淆

通线后需确认：
- 寄存器地址是否正确
- 返回值是有符号/无符号
- 单位换算是否需要除以 10/100/1000（通过 `BarometerPressureScale` 配置）

### 2.2 IO 通讯（Modbus TCP）

- 实现类：[ModbusTcpIoController.cs](file:///e:/Project/BarometerWinform/BarometerWinform/Services/ModbusTcpIoController.cs)
- DI：Input Register（功能码 0x04），默认从 0x1000 开始
- DO：Holding Register（功能码 0x03/0x06），默认从 0x2000 开始

当前实现假设：
- 每 16 路点位打包在 1 个寄存器里（bit0=第1路）
- 单点写 DO 使用“读-改-写”的方式修改某一 bit

给新手的解释：
- “一个寄存器=16位”，可以把它想象成 16 个小开关（bit0~bit15）
- 读取到的寄存器值是一个数字，比如 0x0005（二进制 0000 0000 0000 0101）
  - bit0=1 表示第1路 ON
  - bit2=1 表示第3路 ON
- 写单点输出时不能直接写 0x0001，因为会把其它位清零
  - 所以要先读出原值 current
  - 再只改其中一个 bit
  - 再写回 newValue（这就是“读-改-写”）

通线后需确认：
- 位序是否符合（bit0 是否为通道 1）
- NPN 输入是否需要逻辑取反（某些模块会把 NPN“低有效”在寄存器里转换为“1=导通”）
- DO 区域是否确实是 Holding Register（部分模块可能用 Coil）

## 3. 显耀 IO 点位映射（来自 显耀IO.xlsx）

显耀 IO 表已验证规律如下（与 IoMapBuilder 一致）：
- 真空负压表-1~72：X000 ~ X107
- 真空电磁阀-1~72：Y000 ~ Y107
- 载台上电-1~72：Y110 ~ Y217

程序内部使用“连续编号”：
- 输入：1~72
- 输出：73~216

某 deviceId 的输出编号：
- 真空阀 outputId = TotalInputs + deviceId（默认 72+deviceId）
- 载台电 outputId = TotalInputs + TotalBarometers + deviceId（默认 72+72+deviceId）

映射逻辑参考：[IoMapBuilder.cs](file:///e:/Project/BarometerWinform/BarometerWinform/Services/IoMapBuilder.cs)。

## 4. 配置项（App.config）

配置文件： [App.config](file:///e:/Project/BarometerWinform/BarometerWinform/App.config)

关键开关：
- `UseMockCommunication`
  - true：走 Mock（不需要接线）
  - false：走真实通讯（需要串口+网口）

常用配置：
- 串口：`PortName/BaudRate/DataBits/StopBits/Parity`
- IO：`PlcAddress/PlcPort/IoUnitId`
- DI/DO 地址：`IoInputRegisterStartAddress/IoOutputRegisterStartAddress`
- 气压寄存器：`BarometerPressureRegisterAddress/BarometerPressureScale`
- 报警阈值：`AlarmPressureThresholdPa/AlarmWhenPressureHigherThanThreshold`

## 5. 推荐通线验证顺序（现场调试）

1. 先只验证 TCP：用 ModbusTCPTest 工程对 GX-CL140 做连接、读 DI、写 DO（避免串口干扰）
2. 再验证串口 RTU：逐个从站地址读取压力寄存器，确认单位与缩放
3. 最后联动：把 `UseMockCommunication=false`，观察 UI 的 IO 框状态与实际 IO 是否一致
4. 触发报警：人为制造真空不足/传感器动作，确认程序只在报警进入时下发一次“关阀/断电”
